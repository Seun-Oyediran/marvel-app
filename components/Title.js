import React from 'react'

const Title = ({ title }) => {

    return (
        <div className='big-title' >
            <h1>
                {title}
            </h1>
        </div>
    )
}

export default Title
